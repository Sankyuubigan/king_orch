# 📜 Правила проекта King Orch

## 1. Архитектура

### Стек
- **Backend**: Rust (Tauri 2.0) — 3-слойная архитектура: API → Домен → Инфра
- **Frontend**: TypeScript + Vite — контроллеры + двери (index.ts)
- **Модели**: только локальные `.gguf` файлы (llama.cpp через llama-cpp-2)
- **Агенты**: Markdown файлы (`.md`) с YAML frontmatter

### Трёхслойная архитектура бэкенда
```
api/      — Tauri-команды (тонкий слой, делегирует в domain/infra)
domain/   — Бизнес-логика (оркестратор, парсеры, агенты)
infra/    — Инфраструктура (LLM, сессии, MCP, конфиг)
```
- `api/` импортирует из `domain/` и `infra/` ТОЛЬКО через их двери (`mod.rs`)
- `domain/` импортирует из `infra/` ТОЛЬКО через дверь (`mod.rs`)
- `infra/` не импортирует другие слои

### Двери (Frontend)
Каждая папка-модуль имеет `index.ts` — единственный файл, который другие модули могут импортировать:
- `src/ui/index.ts` — UI-компоненты
- `src/services/index.ts` — сервисы
- `src/utils/index.ts` — утилиты
- `src/controllers/index.ts` — контроллеры

**Правило:** Контроллеры импортируют UI/сервисы/утилиты через двери, не из внутренних файлов.

### Двери (Backend)
Каждый слой имеет `mod.rs` с реэкспортом только публичного API:
- `api/mod.rs` — экспортирует команды и AppState
- `domain/mod.rs` — экспортирует run_chat, AgentProfile, парсеры
- `infra/mod.rs` — экспортирует LlamaEngine, ChatMessage, AppConfig, SessionManager и т.д.

**Правило:** Слой импортирует другой слой только через его `mod.rs`.

### Принципы
- **Изоляция контекста**: каждый агент видит только то, что ему нужно
- **Внешняя память**: [СОСТОЯНИЕ СЕССИИ] (dossier) — двумерное KV-хранилище с неймспейсами
- **Единый стейт (фронтенд)**: Запрещено дублировать переменные в контроллерах. Всё в `Store`.

## 2. Состояние сессии (Досье)

### Формат
`HashMap<String, HashMap<String, String>>` (Rust) / `Record<string, Record<string, string>>` (TS):
- Первый ключ (namespace) — контекст/проблема, по умолчанию `"main"`
- Второй ключ (agent_id) — ID worker-агента
- Значение — текстовый результат

### Правило изоляции
- Агент в `"main"` видит все неймспейсы
- Агент в конкретном неймспейсе видит только свой + `user_query` из `"main"`

## 3. Логика вызова сабагентов (Backend)

За вызовы отвечает **domain-слой** (`domain/orchestrator/`):
- `mod.rs` — цикл обработки: парсинг ответа LLM, вызов сабагентов/инструментов
- `prompt.rs` — сборка системного промпта
- `runtime.rs` — загрузка MCP-серверов

Фронтенд лишь отправляет `invoke("chat_request")` и слушает Tauri-события.

## 4. Система агентов

### Режимы (`mode`)
- `primary` — общается с пользователем, виден в UI
- `router` — маршрутизатор, ВСЕГДА отвечает JSON с `target`
- `worker` — специалист, ответ сохраняется в `dossier[namespace][agent_id]`

### Вызов сабагентов
```json
{"thought": "...", "target": "id", "task_or_response": "задача"}
```
С неймспейсом:
```json
{"thought": "...", "target": "id", "task_or_response": "задача", "namespace": "problem_1"}
```

### Вызов инструментов (MCP)
```json
{"thought": "...", "tool": "имя", "arguments": {...}}
```

## 5. Код-стайл

### Rust (backend)
- Функции не длиннее 50 строк — декомпозируй
- `unwrap()` вне тестов — потенциальный баг. Используй `?` или `map_err`
- Импорты между слоями — ТОЛЬКО через двери (`mod.rs`)

### TypeScript (frontend)
- Контроллеры НЕ импортируют друг друга — общение через шину
- Импорты из других модулей — ТОЛЬКО через двери (`index.ts`)
- UI-компоненты — чистые функции, без стейта
- `invoke()` к Tauri инкапсулирован в Сервисах (где есть сервисная обёртка)

### Markdown (агенты)
- Frontmatter YAML между `---`
- Include-макрос: `<<INCLUDE: database/file.md>>`

## 6. Протокол ошибок

- Каждый неочевидный путь кода должен логироваться
- Запрещено глушить ошибки пустыми `catch` или `unwrap()` без обработки

## 7. Сессии и хранение

- Сессии хранятся как JSON в `app_data_dir/sessions/`
- Dossier — двумерный: `namespace → agent_id → результат`
- Draft — автосохранение с debounce 500ms
- Обратная совместимость: старые сессии мигрируются автоматически

## 8. MCP-серверы

- Расположение: `src-tauri/mcp_servers/`
- Формат: `.cjs` (Node.js) или `.ts` (Deno)
- Протокол: JSON-RPC 2.0 через stdin/stdout
- Базовый фреймворк: `mcp_base.cjs`

## 9. Сборка и релиз

- `build.bat` — локальная сборка
- `release.bat` — релизная сборка с подписью
- Версия: `YY.MM.PATCH` (автоинкремент при релизе)