При экстремальном квантовании (до 1-2 бит) в математической матрице весов возникает «шум». Из-за этого шума вероятности токенов искажаются, и модель случайно «хватает» мусорные токены (иероглифы, бесконечные повторения слов, бессвязный бред). 

Чтобы сделать текст идеально читаемым, ровным и без багов, вам нужно применить **«Современный стек сэмплирования» (Modern LLM sampling stack)** и пару архитектурных фиксов. Вот общепризнанные золотые стандарты настройки:

### 1. Главное оружие: Сэмплер `min_p` (вместо `top_p` и `top_k`)
Раньше все настраивали температуру. Сейчас стандарт для квантованных моделей — это сэмплер **min_p** (Minimum Probability).
* **Как это работает:** Он динамически отсекает все токены-кандидаты, вероятность которых ниже определенного процента от вероятности *самого лучшего* токена. Шум от квантования обычно находится в «хвосте» вероятностей (модель думает: «а может вставить иероглиф?» с вероятностью 4%). `min_p` безжалостно отрезает этот хвост.
* **Что делать:** Отключите `top_k` (поставьте 0) и `top_p` (поставьте 1.0). Включите `min_p` на значение **от 0.05 до 0.1**. Температуру при этом можно вернуть на нормальные `0.8` или `1.0` — текст снова станет красивым, креативным, но без галлюцинаций.

### 2. Сэмплеры DRY и XTC (Защита от зацикливания)
Обычно перед тем, как Qwen переходит на иероглифы, он начинает повторять одни и те же конструкции. Стандартный `repetition_penalty` часто ломает грамматику русского языка (слова теряют окончания).
* **DRY (Don't Repeat Yourself):** Новый идеальный сэмплер, который наказывает модель только за явные бесконечные циклы текста. 
* **XTC:** Продвинутая альтернатива температуре, заставляющая модель использовать разнообразные слова, но не уходящая в бред. Если ваш лоадер (LM Studio, text-gen-webui, llama.cpp) их поддерживает — обязательно активируйте.

### 3. Жесткая фиксация Chat Template (Формат чата)
Вы не поверите, как часто иероглифы лезут просто из-за того, что программа неправильно оборачивает ваш промпт. Qwen использует специфический шаблон форматирования — **ChatML**.
* **Проблема:** Если стоит «Auto» или шаблон от Llama, модель не видит правильных скрытых тегов остановки генерации (`<|im_start|>` и `<|im_end|>`) и сходит с ума, выдавая мусорный текст.
* **Что делать:** В настройках (LM Studio, Ollama или другой программы) жестко переключите *Chat Template / Prompt Format* с `Auto` или `Default` на **ChatML** (или специально Qwen). Это лечит до 70% случаев языковых сбоев.

### 4. Не квантуйте KV-кэш (Тип памяти bf16)
В квантованных моделях контекст (то, что вы уже обсудили) хранится в так называемом KV Cache. Некоторые программы пытаются сжимать (квантовать) еще и его (в форматы q8_0 или q4) для экономии видеопамяти.
* Для сильно сжатой модели 27B сжатие еще и памяти — фатально для логики. 
* **Что делать:** В настройках llama.cpp укажите флаги `--cache-type-k bf16` и `--cache-type-v bf16` (или просто снимите галочки "KV cache quantization" в интерфейсе). Модель заберет чуть больше памяти, но логика психотерапевта станет бетонной.

### Идеальный сетап настроек для вашей задачи
Если вы используете LM Studio / Text-Generation-WebUI / llama.cpp, выставьте параметры именно в таком виде:

* **Temperature:** `0.8` (или даже `1.0`) — оставляем креативность для хорошего текста.
* **Min_p:** `0.05` или `0.08` — **(критически важно!)** отрезает иероглифы.
* **Top_k:** `0` (отключить).
* **Top_p:** `1.0` (отключить).
* **Repetition Penalty:** `1.0` или `1.05` (почти отключено, чтобы не портить русский язык).
* **DRY / XTC:** Включить, если есть в интерфейсе.
* **Prompt Format:** `ChatML`.

**Итог:** Применяя `min_p` вместо старых ограничителей, вы лечите не "симптомы", а саму математику проблемы сжатых весов. Шум квантования (китайский язык) больше не пройдет фильтр генерации, и текст будет читаться именно так, как вы задумали для эмпатичного психотерапевта.